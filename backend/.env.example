PORT=3000
# Mode d'exécution :
#   development → tous les logs s'affichent (recommandé en dev)
#   production  → logs de routine coupés (PC 2 Go RAM / HDD)
NODE_ENV=development
DATABASE_URL=postgresql://USER:PASSWORD@127.0.0.1:5432/gestion_dossiers
JWT_SECRET=changez_moi_par_une_cle_secrete_longue
JWT_EXPIRES_IN=24h
UPLOAD_DIR=uploads
CORS_ORIGIN=http://localhost:9000
